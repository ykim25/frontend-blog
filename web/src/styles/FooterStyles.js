import styled from 'styled-components';

export const FooterStyles = styled.footer`
  padding: 5rem 0 2rem 0;
  text-align: center;

  .copyright {
    margin-top: 1rem;
    color: var(--gray);
    font-size: 1.5rem;
  }
`;
